---
version: 0.2.0
shell: zsh
build:
  - repo:
      path: ~/dotfiles
      url: https://github.com/jcthomassie/dotfiles.git
  - repo:
      path: ~/yurt
      url: https://github.com/jcthomassie/yurt.git

  # OS specifics
  - case:
    # Ubuntu only
    - positive:
        spec: { distro: ubuntu }
        include:
          - require: [ apt, apt-get ]
          - install:
            - name: build-essential
            - name: libssl-dev
            - name: pkg-config
    # Windows only
    - positive:
        spec: { platform: windows }
        include:
          - require: [ choco ]
          - link:
            - tail: ${{ dotfiles.path }}/pwsh/profile.ps1
              head: ~/Documents/WindowsPowerShell/Microsoft.PowerShell_profile.ps1
            - tail: ${{ dotfiles.path }}/.wt.json
              head: ${{ env.LocalAppData }}/Packages/Microsoft.WindowsTerminal_8wekyb3d8bbwe/LocalState/settings.json
            - tail: ${{ dotfiles.path }}/python/.flake8
              head: ~/.flake8
    # Posix only
    - negative:
        spec: { platform: windows }
        include:
          - require: [ brew ]
          - run: brew bundle --file ${{ dotfiles.path }}/.brewfile
          - install:
            - name: zsh
            - name: exa
          - link:
            - tail: ${{ dotfiles.path }}/zsh/.zshenv
              head: ~/.zshenv
            - tail: ${{ dotfiles.path }}/python/.flake8
              head: ~/.config/flake8

  # Install rust toolchain
  - require: [ cargo ]
  - install:
    - name: cargo-edit
      managers: [ cargo ]

  # Install essential packages
  - install:
    - name: git
    - name: bat
    - name: vim
    - name: gh
    - name: zoxide
    - name: delta
      aliases: { brew: git-delta, cargo: git-delta }

  # Apply symlinks
  - matrix:
      values:
        tail:
          - .vimrc
          - git/.gitconfig
          - git/.gitignore_global
          - python/.mypy.ini
          - python/.isort.cfg
        head:
          - ~/.vimrc
          - ~/.gitconfig
          - ~/.gitignore_global
          - ~/.mypy.ini
          - ~/.isort.cfg
      include:
        - link:
          - tail: ${{ dotfiles.path }}/${{ matrix.tail }}
            head: ${{ matrix.head }}

  # Amazon specifics
  - case:
    - positive:
        spec: { user: thomajl }
        include:
          - link:
            - tail: ${{ dotfiles.path }}/git/.gitconfig.amazon
              head: ~/.gitconfig.local
    - default:
        include:
          - link:
            - tail: ${{ dotfiles.path }}/git/.gitconfig.default
              head: ~/.gitconfig.local
