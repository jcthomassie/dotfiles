# Takes one argument:
#   - Workspace directory name (i.e. mvms/)

nds_add () {
    local LOCAL_DIR=$1
    local CLOUD_DIR="$CLOUD_WORKSPACE/$1"

    ninja-dev-sync --add "$LOCAL_DIR" --add-host "$CLOUD_DESK_DNS" --add-remote "$CLOUD_DIR" -setup
}

nds_add "$@"
