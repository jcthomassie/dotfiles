---
repo:
  local: $HOME/dotfiles
  remote: https://github.com/jcthomassie/dotfiles.git

build:
  - link:
    - tail: $DOTS_REPO_LOCAL/.zsh/.zshenv
      head: $HOME/.zshenv
    - tail: $DOTS_REPO_LOCAL/.gitconfig
      head: $HOME/.gitconfig
    - tail: $DOTS_REPO_LOCAL/.gitignore_global
      head: $HOME/.gitignore_global
    - tail: $DOTS_REPO_LOCAL/.vimrc
      head: $HOME/.vimrc

  # Link local gitconfig
  - case:
    - local:
        spec:
          user: thomajl
        build:
          - link:
            - tail: $DOTS_REPO_LOCAL/.gitconfig.local##user.thomajl
              head: $HOME/.gitconfig.local

    - default:
        build:
          - link:
            - tail: $DOTS_REPO_LOCAL/.gitconfig.local##default
              head: $HOME/.gitconfig.local

  # Install local packages
  - case:
    - local:
        spec:
          distro: ubuntu
        build:
          - install:
            - name: build-essentials
              managers:
              - apt

  # Bootstrap rustup
  # Bootstrap brew

  # Install essential packages
  - install:
    - name: zsh
      managers:
      - brew
      - apt-get
    - name: git
      managers:
      - brew
      - apt
      - choco
    - name: git-delta
      managers:
      - brew
      - cargo
    - name: delta
      managers:
      - choco
    - name: bat
      managers:
      - brew
      - choco
      - cargo
      - apt
